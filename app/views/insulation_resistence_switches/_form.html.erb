<%= form_for(insulation_resistence_switch) do |f| %>
  <% if insulation_resistence_switch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(insulation_resistence_switch.errors.count, "error") %> prohibited this insulation_resistence_switch from being saved:</h2>

      <ul>
      <% insulation_resistence_switch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phase %>
    <%= f.number_field :phase %>
  </div>

  <div class="field">
    <%= f.label :proof %>
    <%= f.number_field :proof %>
  </div>

  <div class="field">
    <%= f.label :position_switch %>
    <%= f.check_box :position_switch %>
  </div>

  <div class="field">
    <%= f.label :connection %>
    <%= f.text_field :connection %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
