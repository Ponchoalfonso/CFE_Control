<%= form_for(battery_bank) do |f| %>
  <% if battery_bank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(battery_bank.errors.count, "error") %> prohibited this battery_bank from being saved:</h2>

      <ul>
      <% battery_bank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
