<%= form_for(subestation) do |f| %>
  <% if subestation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subestation.errors.count, "error") %> prohibited this subestation from being saved:</h2>

      <ul>
      <% subestation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :abreviation %>
    <%= f.text_field :abreviation %>
  </div>

  <div class="field">
    <%= f.label :identifier %>
    <%= f.number_field :identifier %>
  </div>

  <div class="field">
    <%= f.label :zone %>
    <%= f.select(:zone_id, options_for_select(@zones), {}, :class => "selectArea") %>
  </div>

  <div class="actions send" id="zones">
    <%= f.submit %>
  </div>
<% end %>
