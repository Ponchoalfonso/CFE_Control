<%= form_for(hot_collar_switch) do |f| %>
  <% if hot_collar_switch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hot_collar_switch.errors.count, "error") %> prohibited this hot_collar_switch from being saved:</h2>

      <ul>
      <% hot_collar_switch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phase %>
    <%= f.text_field :phase %>
  </div>

  <div class="field">
    <%= f.label :nozzle %>
    <%= f.number_field :nozzle %>
  </div>

  <div class="field">
    <%= f.label :mv %>
    <%= f.text_field :mv %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
