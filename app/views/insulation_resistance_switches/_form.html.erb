<%= form_for(insulation_resistance_switch) do |f| %>
  <% if insulation_resistance_switch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(insulation_resistance_switch.errors.count, "error") %> prohibited this insulation_resistance_switch from being saved:</h2>

      <ul>
      <% insulation_resistance_switch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phase %>
    <%= f.text_field :phase %>
  </div>

  <div class="field">
    <%= f.label :proof %>
    <%= f.text_field :proof %>
  </div>

  <div class="field">
    <%= f.label :switch_position %>
    <%= f.check_box :switch_position %>
  </div>

  <div class="field">
    <%= f.label :connection %>
    <%= f.text_field :connection %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
