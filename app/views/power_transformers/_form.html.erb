<%= form_for(power_transformer) do |f| %>
  <% if power_transformer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(power_transformer.errors.count, "error") %> prohibited this power_transformer from being saved:</h2>

      <ul>
      <% power_transformer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
